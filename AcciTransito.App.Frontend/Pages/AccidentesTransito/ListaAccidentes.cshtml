@page
@model AcciTransito.App.Frontend.Pages.ListModel5
@{
}

<h4>Accidentes</h4>
<table class="table table-bordered">
    <caption>Información de Accidentes</caption>

 <th scope="col" > Número de Accidente</td>
 <th scope="col" > Tipo de Accidente</td>
 <th scope="col" > Lista de Personas</td>
  <th scope="col" > Lista de Vehiculos</td>
<tbody>
@* @if (Model.Count() > 0)
{
        foreach (var item in Model) { }
}
else
{
        <p>No existen datos</p>
} *@
    @foreach (var acci in Model.accidentes)

    {

        <tr>

            <td> @acci.NumeroAccidente</td>
            <td> @acci.TipoAccidente</td>

            <td> <a class="btn btn-success" asp-page="/AccidentesTransito/ListaPersonas" asp-route-id="@acci.id">
                <i class="fas fa-male"></i></a></td>

            <td> <a class="btn btn-success" asp-page="/AccidentesTransito/ListaVehiculos" asp-route-id="@acci.id">
                <i class="fas fa-car"></i></a></td>



                <td><a   asp-page="/AccidentesTransito/DetalleAccidente" asp-route-id="@acci.id">  <i class="fas fa-list-alt">Detalle</i></a> </td>

                <td><a  asp-page="/AccidentesTransito/EditarAccidente" asp-route-id="@acci.id">
                <i class="fas fa-car-crash " aria-hidden="true">Editar</i></a></td>
               </tr>

  }
  </tbody>
</table>

<a asp-page="/AccidentesTransito/EditarAccidente" class="btn btn-success"><i class="fas fa-folder-plus">- Crear Accidente</i></a>




